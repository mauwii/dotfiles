#!/usr/bin/env zsh

# check if this scritp was sourced
if [[ ! $zsh_eval_context == *"file"* ]]; then
    echo "Error: Script must be sourced!"
    exit 1
fi

# zstyle ':completion:*' completer _extensions _complete
# zstyle ':completion:*' completer _expand _complete _match _oldlist _ignored
# zstyle ':completion:*' completer _complete _oldlist _extensions

# sort completions
# zstyle ':completion:*:*:command-:*:*' group-order alias builtins functions commands

# # use cache
# zstyle ':completion:*' use-cache true
# # zstyle ':completion:*' cache-path "${HOME}/.zcompdump-$(hostname -s)-${ZSH_VERSION}"

# use menu selection
zstyle ':completion:*' menu select

#Set Styles
# zstyle ':completion:*:*:*:*:descriptions' format '%B-- %d --%b'

# style for no matches
# zstyle ':completion:*:messages' format ' %F{purple} -- %d --%f'
zstyle ':completion:*:warnings' format ' %F{yellow}-- no matches found --%f'

# detailed filelist
# zstyle ':completion:*' file-list all
# zstyle ':completion:*:default' list-colors ${(s.:.)LS_COLORS}

# Directories
zstyle ':completion:*:*:cd:*' tag-order local-directories directory-stack path-directories
zstyle ':completion:*:*:cd:*:directory-stack' menu yes select
zstyle ':completion:*:-tilde-:*' group-order 'named-directories' 'path-directories' 'users' 'expand'
zstyle ':completion:*' squeeze-slashes true
